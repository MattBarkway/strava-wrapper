// pub async fn get(token: &str, id: &str) {
//     query::get(&format!("uploads/{}", id), token).await
// }

pub async fn create() {
    // TODO query::form function
    // query::form("uploads", token, &activity).await
}
